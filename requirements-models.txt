# Local AI generation — install on a machine with an NVIDIA GPU (8 GB+ VRAM).
#
#   pip install -r requirements.txt -r requirements-models.txt
#
# ACE-Step: full songs with vocals + lyrics, or instrumentals when the
# lyrics are left empty (Apache-2.0, watermark-free, commercial OK).
# Pulls in its own torch/transformers/accelerate dependencies.
ace-step @ git+https://github.com/ace-step/ACE-Step.git
