# AI model backends — install on a machine with an NVIDIA GPU (8 GB+ VRAM).
#
#   pip install -r requirements.txt -r requirements-models.txt
#
# ACE-Step: full songs with vocals + lyrics (Apache-2.0, commercial OK).
ace-step @ git+https://github.com/ace-step/ACE-Step.git

# MusicGen via transformers: instrumental music (CC-BY-NC weights).
torch>=2.2
transformers>=4.40
accelerate>=0.29
